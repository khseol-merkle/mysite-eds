version: 1
indices:
  site:
    include:
      - '**'
    exclude:
      - '**/drafts/**'
    target: /query-index
  repo:
    target: /repo-index
    properties:
      lastModified:
        select: none
        value: parseTimestamp(headers["last-modified"], "ddd, DD MMM YYYY hh:mm:ss GMT")
